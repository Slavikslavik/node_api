<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/head'); %>
    </head>
    
<body>
    <header>
        <%- include('../partials/header', { auth } ); %>
    </header>
    <p>hi</p>
    <% if(auth) {%>

         <% if(users) {%>
            <ul>
        <% for(let i=0; i<users.length; i++) {%>
           <li><%= users[i].username %> <a href="/chats">go to chat</a></li>
        <% } %>
        </ul>
        <%}%>
        <%} else { %>
            <ul>
                <% for(let i=0; i<users.length; i++) {%>
                   <li><%= users[i].username %></li>
                <% } %>
                </ul>
        <%}%>



    <footer>
        <%- include('../partials/footer'); %>
    </footer>

</body>
</html>
